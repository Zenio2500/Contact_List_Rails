<p style="color: green"><%= notice %></p>

<h1>Contatos</h1>

<div id="contacts">
  <% @contacts.each do |contact| %>
    <%= render "contact" %>
    <p>
      <%= link_to "Exibir este contato.", contact %>
    </p>
  <% end %>
</div>

<%= link_to "Novo contato.", new_contact_path %>
